配方（2026-09-21 test实测，目标单1431224精确复现，103/500=20.6%命中）：前置：平台云打印单已取号（printDataId/carrierService有值、waybillGetStatus=2）、leadingSts=300、独占波次；test复现专用需临时关闭Apollo开关 wms.outbound.shipment-rollback.duplicate-check.enabled=false（outbound-api）与 wms.shipping.trigger-cancel.restore-print-fields.enabled=false（wms-api），复现完置回true。四技巧：1) 位置排布——第二批目标单第1位先cancel成功（leadingSts=100），第一批目标单约第100位后到撞回池触发MSG_SHIP_0033；2) 备份方向性——第一批消息备份是清空前快照含原值，第二批在第一次清空后才组装备份已空，只有第一批的恢复能补回原值；3) 时序窗口——第一批失败必须早于第二批消息cleanup（重置processType=NORMAL），否则恢复SQL where processType='ROLLBACK'匹配0行；第二批要足够大（400+）拉长cleanup，第一批目标单位置不能太靠后（实测500单约99s跑完）；4) 独占波次——共享波次两条消息互抢波次锁双双MSG_SHIP_0082失败，空备份恢复先跑会把运单号置空。失败变体排查：目标单位置太靠后→运单号不补回；两批同序竞争→第二批失败。验证SQL：SELECT id,code,carrierService,printDataId,primaryWaybillCode,waybillGetStatus,leadingSts,processType FROM shipment_header WHERE id=<目标>; 命中特征：primaryWaybillCode=原值、waybillGetStatus=2、leadingSts=100、printDataId IS NULL AND carrierService IS NULL。观察点：Kibana关键字'恢复processType'（中文长句搜不到），warn里waybillCode非空且printDataId=null即命中。短路坑（test核对教训）：用户给的订单池状态单（leadingSts=100）回退走'ShipmentRollbackBizService:278 [订单回退-回到待取号]订单池状态出库单仅清空运单号，不发送取消MQ'分支，无并发cancel场景——复现前先确认单据状态和打印字段非空。
