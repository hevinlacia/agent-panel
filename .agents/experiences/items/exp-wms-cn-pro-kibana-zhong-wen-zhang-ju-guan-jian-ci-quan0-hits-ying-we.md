实战结论（2026-09-21 cn-pro排查INC-122）：1) 可靠关键字清单：恢复processType（WARN一条含shipmentId/waybillCode/waybillGetStatus/printDataId/carrierService/cancelError全字段——排查cancel恢复首选）；role=producer（发送侧，含[订单回退-回到待取号]发送取消MQ…shipmentIds=[…]）；RocketMQ消息消费开始/结束（含完整shipmentIdList）；完整业务单号（SO…/shipmentId/完整运单号，ES整token分词，前缀不命中、不支持OR）；DTS旧值用运单号或wms-dts。2) 全0 hits的写法：中文长句（RocketMQ订单回退cancel业务失败）、中文词组（发送取消MQ/回到待取号/订单回退/回退cancel/业务失败）——CN PRO分词下都搜不到，不要用。3) --app过滤不可信：appName.keyword精确匹配，CN PRO部分应用实际写入appName与规范名不一致（如--app yl-cwhsea-wms-outbound-api 0 hits，去掉--app同关键字1051 hits）——先去--app跑通，再从返回hits的appName字段发现真实值。4) 通用纪律：0命中时先跑同窗口无关键字broad query确认索引有数据，再换关键字/去--app；时间用带时区绝对窗口。相关skill：kibana-log-query（已内置--clip/--dump-json/Batch规范与--app Known Issue，见其SKILL.md）。
