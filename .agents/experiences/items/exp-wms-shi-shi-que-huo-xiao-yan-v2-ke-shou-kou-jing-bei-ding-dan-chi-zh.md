根因机制（cn-pro 实证，WMS-INC-120 定案）：实时缺货判断（syncStockout=1）读 qh:{仓}:{货主} 缓存；V2 可售 = onHand - frozen - 未拣货池占用，未拣货池 = shipment_header.trailingSts<=300 AND processType IN (NORMAL,PENDING) 的 planQty 合计（yl-cwhsea-wms-api CompanySettingsService.groovy getUsableQtyNewV2 ~L675，池 SQL 无状态/时间兜底；getUsableQty L538-556 灰度判断被注释，commit 1c8eedfc1b 2026-08-11 起全仓强制 V2）。
机制链：长期 PENDING 卡单（SO26070500373614，2026-07-05 创建挂起两月，planQty=1）占满唯一可用量 → 货主参数页开启实时缺货判断（/wms/settings/company/setItemRedis flag=1 → updateRedisInventory→initCache）把 field 420310784030733163_ZP 写成 0（1-0-1）甚至 -1（池=2 时 1-2，负数直接入缓存）→ shipment-api 实时校验读 field<=需求 → MSG_INTF_0207 拒单，PDD 回传 rejected(stock_invalid)。
口径分裂（页面有货却拒单的原因）：页面库存明细可用量 = location_inventory 行内算术 onHand-allocated-locked-frozen（yl-cwhsea-wms-api LocationInventoryService.groovy L1087/L1442，不 join shipment 表）；PENDING 卡单处于「已占池、未分配」中间态，allocatedQty=0 在库存表上隐形 → 页面可用 1 与校验可售 0 同时成立，两套算法各自都对，不是数据损坏。
与 INC-119（exp-wms-redis-db-msg_intf_0207）区分：同 MSG_INTF_0207 报错签名，INC-119=field 缺失/initCache 未刷新型；INC-120=field 被正确写入 0/-1 型。校验日志 [[SKU, ZP, -1, 0]] 下 field=0 与 field 缺失不可区分，必须拉重建 dump 日志「全量同步完成-数据库计算结果」判定 field 实际值。
修复路径：数据修复为主——业务确认取消/履约卡单释放池 → setItemRedis flag=1 重建缓存回正；代码改进另开需求：V2 池 SQL 卡单时间兜底、缓存值 max(0) 钳制、MSG_INTF_0207 报错附校验上下文、页面增「可售（扣订单池）」列、按本机制复核 INC-119 结论。
证据（均可独立复核）：cn-pro 重建写0 traceId=91991039421005952973498673812623（2026-09-10 14:35:57+0800，最终可售库存 {..._ZP:"0"}+hmset OK）；拒单 tid=5386056823936170245（15:05:26，app=yl-cwh-wms-shipment-admin）；写-1 traceId=96766207933660910860008147936134（15:48:54，orderPoolCount=2）；DB 池查询 SELECT ... FROM shipment_header a JOIN shipment_detail b ... WHERE a.trailingSts<=300 AND a.processType IN (NORMAL,PENDING) 返回卡单 2 行（SO26070500373614 JYCK PENDING 2026-07-05 + SO26091001571628）。
平台级：9/8 起同签名失败 10 万+，CN/SEA 跨区域（SEA PRO VN-EAW-01 出库单自动定位 availableCount:0 同日出现，app=yl-cwhsea-wms-outbound-api）；「池占用压 0/负」为一型，其他货主可能存在未初始化/增量漂移型，分型待抽样。
