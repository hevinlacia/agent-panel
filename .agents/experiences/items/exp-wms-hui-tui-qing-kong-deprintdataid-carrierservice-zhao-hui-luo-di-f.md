可行链路：1) 定位清空时刻（回退操作时间，从process_history或发送侧日志拿），CN PRO窗口2026-09-20 08:58:30-09:01:00；2) log-api（yl-cwhsea-wms-log-api WmsDtsSyncListener.java）日志含[DTS消息完整内容]，logmsg全量不截断（查询时用kibana-log-query --clip 0 --dump-json保全文）；3) 按完整运单号multi_match phrase逐单检索（ES整token分词，前缀不命中、不支持OR），3-4线程并发，195单约2分钟，结果逐条append jsonl断点续跑；4) 从UPDATE消息old镜像提取printDataId/carrierService（printDataId全局唯一，抽查存在于print_data表，body是加密面单——补回指针即可打印无需重新取号）。字段核对方法：old是全行镜像可精确核对清空前真实值——195单核对secondWaybillCodes/rejectionNote/carrierName清空前全为null（carrierName被置空串是副作用、打印链路不用），primaryWaybillCode/waybillGetStatus已被WMS-072恢复补回，expressBillFlag不在清空列——补数SQL只需printDataId+carrierService两字段；箱表shipping_container_header同补两字段即完整（健康单PDD云打印单箱子本就只存waybillCode+printDataId+carrierService）。被否路径记录：shipment_header/shipping_container_header现值均null；print_data.body为encryptedData密文；WaybillBackup只备份2字段；取号时点outbound-api【获取出库单电子面单】日志对PDD平台单0命中（取号值由OMS推送写入不产生取号日志）——该日志路线仅适用于WMS自身取号的单。交付物范式：390条UPDATE（两表各195）每条带AND printDataId IS NULL幂等保护，执行后验证SQL（缺失计数归0+事发单抽查）；本次已于2026-09-21执行并验证（事发单printDataId=2101463576374689792 lastUpdated=09-21 20:14:30）。
